{
  "name": "coffeehubnepal",
  "version": "1.0.0",
  "description": "CoffeeHubNepal - Coffee Community Platform",
  "type": "module",
  "scripts": {
    "build": "npm run heroku-build",
    "start": "cd apps/api && node dist/server.js",
    "heroku-build": "cd apps/web && npm install --include=dev && npm run build && cd ../api && npm install --include=dev && npm run build && cd ../.. && node scripts/copy-frontend-to-api.js",
    "dev": "cd apps/web && npm run dev",
    "api:dev": "cd apps/api && npm install && npm run dev",
    "api:test": "cd apps/api && npm install && npm test",
    "postinstall": "echo 'Dependencies installed'"
  },
  "engines": {
    "node": "20.x",
    "npm": ">=9.0.0"
  }
}

